const InitialState = []
const chapters = (state = InitialState, action) => {
    switch (action.type) {
        // case ACTION_TYPE_2:
        //     return state
        default:
            return state
    }
}
export default chapters;